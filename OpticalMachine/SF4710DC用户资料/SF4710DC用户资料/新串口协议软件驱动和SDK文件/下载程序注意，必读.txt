注意：MCU程序为空时，第一次下载要使用hex这个文件，可以用STLink工具或者串口线烧录；
	  MCU程序非空时，但是属于旧版本程序（2.0以下），要升级到2.0以上时，使用烧录hex这个文件。

	MCU程序非空时，但是属于新版本程序（2.0以上），可以用SF GUI tool.exe进行升级，选择使用.bin文件升级。
	   
	 ！  两者不能混用， hex 文件这个程序是带有Bootloader程序！
	   
